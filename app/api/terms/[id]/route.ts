import {db} from "@/lib/db";import {getWorkspaceContext} from "@/lib/workspace-context";
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {workspace}=await getWorkspaceContext();const term=await db.term.findFirst({where:{id,workspaceId:workspace.id}});if(!term)return Response.json({error:"术语不存在"},{status:404});await db.$transaction([db.term.delete({where:{id}}),db.auditLog.create({data:{workspaceId:workspace.id,action:"TERM_DELETE",entityType:"Term",entityId:id,detail:{source:term.source}}})]);return new Response(null,{status:204})}
